import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
// If you have a main CSS file, you would import it here, e.g.:
// import './index.css' 

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
